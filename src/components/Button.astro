---
import type {HTMLAttributes} from "astro/types";

interface Props extends HTMLAttributes<"button">{
    theme?: "primary" | "secondary"
}
const {theme = "primary", class:className, ...rest} = Astro.props
---

<button class="button">
    <slot />
</button>

<style is:global lang="scss">
    // manual reset class
    .button {
        --_clr: ;
        --_bg-clr: ;
        --_bdr: ;
    }

</style>